#include <stdio.h>
// Khai bao hang so SOHOCSINH
#define SOHOCSINH 30

int main() {
    // Khai bao hang so so PI
    const float PI = 3.14;
    
    printf("so hoc sinh la: %d\n", SOHOCSINH);
    printf("so pi la: %f", PI);    
               
    return 0;
}
